label: Color Themes
type: structure
columns:
  uid:
    before: "--color-theme-"
  label:
  color_theme:
fields:
  uid:
    label: UID
    type: text
    before: --color-theme-
    converter: slug
    help: >
      never change the UID when background is already in use!
  label:
    label: Label
    type: text
  color_theme:
    label: Color Theme
    type: structure
    fields:
      variable:
        type: select
        label: Variable
        options:
          color: --color
          background_color: --background-color
          border_color: --border-color
          outline_color: --outline-color
          box_shadow_ambient_color: --box-shadow-ambient-color
          box_shadow_direct_color: --box-shadow-direct-color
      color:
        type: text
        label: Color