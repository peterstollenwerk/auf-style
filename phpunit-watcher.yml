watch:
  directories:
    - src
    - tests
    - lib
  fileMask: '*.php'
notifications:
  passingTests: false
  failingTests: true
# phpunit:
#   binaryPath: vendor/bin/phpunit
#   arguments: '--stop-on-failure'
#   timeout: 180